# abort script on any command that exits with a non zero value
set -e

NPROC="$(nproc)"

cd check
tar xfz check_0.10.0.orig.tar.gz
cd check-0.10.0

./configure --prefix=${BOSH_INSTALL_TARGET}
make -j "${NPROC}"
make -j "${NPROC}" install
